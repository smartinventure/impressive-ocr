node_modules/
dist/
build/
out/
coverage/
.venv/
sidecar/
pnpm-lock.yaml
*.md

# Reference material and design output — kept verbatim so it can be diffed against upstream.
_resources/

# Editor and agent scratch state, not repository content.
.claude/

# Generated by drizzle-kit. Formatting these achieves nothing: the next `pnpm db:generate`
# rewrites them in drizzle's own style and the check fails again on the same files.
packages/db/migrations/

# Ground-truth transcripts and fixtures for the accuracy measurements. Data, not source —
# reflowing it would make every future diff against a corrected transcript unreadable.
samples/
